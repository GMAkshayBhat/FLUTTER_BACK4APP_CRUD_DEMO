name: flutter_back4app_crud
description: A Flutter application with Back4App integration.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  parse_server_sdk_flutter: ^3.1.0
  cupertino_icons: ^1.0.2
  image_picker: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true